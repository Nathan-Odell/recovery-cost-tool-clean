import React from 'react';
import ReactDOM from 'react-dom/client';
import RecoveryCostTool from './RecoveryCostTool';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RecoveryCostTool />
  </React.StrictMode>
);